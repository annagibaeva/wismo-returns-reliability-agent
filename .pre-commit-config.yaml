repos:
  - repo: local
    hooks:
      - id: lexicon-freeze
        name: lexicon freeze (held-out discipline)
        entry: python eval/check_lexicon_freeze.py
        language: system
        pass_filenames: false
        always_run: true
