# Copy to .env (or export in your shell) to enable the Claude tool-use backend.
# Without a key, everything runs on the deterministic backend — no key required.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the model used by the LLM backend.
AGENT_MODEL=claude-opus-4-8
